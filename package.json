{
  "name": "thr",
  "version": "1.0.0",
  "scripts": {
    "prepare": "pika-web --dest=src/web_modules"
  },
  "test": "jest",
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "typescript": "^3.6.4"
  },
  "dependencies": {
    "idb": "^4.0.4",
    "stemr": "^1.0.0",
    "ts-jest": "^24.1.0"
  },
  "@pika/web": {
    "webDependencies": [
      "idb",
      "stemr"
    ]
  }
}
